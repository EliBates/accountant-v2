const TopControl = () => {
  return (
    <div className="flex justify-between items-center">
      <button>Project</button>

      <button>User</button>
    </div>
  );
};

export default TopControl;
